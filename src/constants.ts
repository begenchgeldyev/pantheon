// Constants shared across modules.

/**
 * The owner's pre-existing OpenClaw agent. Used both when registering the
 * owner (provisioner) and as the fallback agent for /notify payloads written
 * before multi-user support.
 */
export const MAIN_AGENT_ID = "main";
